name: Docker build

on:
  pull_request:
      types: [opened, synchronize, reopened]
      
jobs:
  docker:
    uses: usdot-fhwa-stol/actions/.github/workflows/docker.yml@feature-add-runner-input
    with:
      runner: ubuntu-latest-16-cores
